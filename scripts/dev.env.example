# OverDraft Local Development Environment
# Copy this file to dev.env and configure your settings

# =============================================================================
# REQUIRED
# =============================================================================

# Google API Key (REQUIRED - script will not start without it)
# Get from: https://console.cloud.google.com/apis/credentials
# 1. Create or select a project
# 2. Enable Google Sheets API
# 3. Create API Key and restrict to Sheets API only
GOOGLE_API_KEY=your_api_key_here

# =============================================================================
# OPTIONAL - Server Settings
# =============================================================================

# API server port (default: 8000)
SERVER_PORT=8000

# Cache TTL in seconds (default: 1)
CACHE_TTL=1

# Rate limiting per IP per spreadsheet (default: 1000/minute)
# High limit is fine since client requests only read from cache
RATE_LIMIT=1000/minute

# =============================================================================
# OPTIONAL - Client Settings
# =============================================================================

# Vite dev server port (default: 3000)
CLIENT_PORT=3000

# =============================================================================
# OPTIONAL - CORS Settings
# =============================================================================

# CORS origins - for local dev, allow all
# Production should use specific origins: ["https://yourdomain.com"]
CORS_ORIGINS=["*"]

# =============================================================================
# OPTIONAL - Load Testing Settings
# =============================================================================

# Test spreadsheet ID (for load testing)
# Default uses a public test spreadsheet
TEST_SPREADSHEET_ID=1b-KhzrlrvNikk9fp5URYQglHE1cOly9ME-NitQdMKBs

# Test sheet GID
TEST_GID=1506748454

