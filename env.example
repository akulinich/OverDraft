# OpenAI API Configuration
# Required: Your OpenAI API key for CrewAI agents
OPENAI_API_KEY=your-openai-api-key-here

# Optional: OpenAI model to use (defaults to "gpt-4o")
# Examples: "gpt-4o", "gpt-4-turbo", "gpt-3.5-turbo"
OPENAI_MODEL=gpt-4o

# Optional: Output directory for generated files (defaults to "./output")
# All generated code, tests, and documentation will be written here
OUTPUT_DIR=./output

# Optional: GitHub token for accessing private repositories or higher API rate limits
# Only needed if you want to use GitHub issue URLs from private repos
# Create a token at: https://github.com/settings/tokens
GITHUB_TOKEN=your-github-token-here

# Optional: CrewAI verbose logging (defaults to "true")
# Set to "true" or "false" to enable/disable detailed agent output
# Can be overridden with --quiet flag in CLI
CREW_VERBOSE=true

