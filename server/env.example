# Google Sheets API Key
# Get from: https://console.cloud.google.com/apis/credentials
# Restrict to Google Sheets API only
GOOGLE_API_KEY=your_api_key_here

# Cache TTL in seconds (how often to refresh data from Google)
CACHE_TTL=1

# Server settings
HOST=0.0.0.0
PORT=8000
DEBUG=false

# CORS origins - IMPORTANT for production security
# Empty or ["*"] = allow all origins (development only!)
# Production: specify exact origins as JSON array
# Example: ["https://yourdomain.com", "https://app.yourdomain.com"]
CORS_ORIGINS=["https://yourdomain.com"]

# Rate limiting
# Format: "requests/period" where period is second, minute, hour, day
# Examples: "60/minute", "1000/hour", "10/second"
RATE_LIMIT=60/minute
